#!/bin/sh
set -e

ip addr add 10.10.100.2/24 dev eth0
ip link set eth0 up
ip route add default via 10.10.100.1 dev eth0

chmod +x /shared/bin/kvcli

echo "client ready"
echo "example: /shared/bin/kvcli -addr 10.10.9.2:9999"
